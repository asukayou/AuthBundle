<div id="menu" class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container">
        <div class="navbar-collapse collapse navbar-responsive-collapse">
            <ul class="nav navbar-nav navbar-right">
                <li class="dropdown"><a href="#" class="dropdown-toggle" data-toggle="dropdown"><span class="glyphicon glyphicon-user"></span> {{ '%name%' | trans({'%name%': app.user.name}) }} <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li><a href="{{ path('password_index') }}">{{ 'Change Password' | trans }}</a></li>
                        {% if is_granted('ROLE_ADMIN') %}
                            <li><a href="{{ path('user_index') }}">{{ 'Manage User' | trans }}</a></li>
                        {% endif %}
                        <li><a href="{{ path('logout') }}">{{ 'Logout' | trans }}</a></li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</div>
