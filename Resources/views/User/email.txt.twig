{% trans with {'%systemName%': systemName, '%userName%': userName, '%indexUrl%': url('index'), '%userId%': userId, '%password%': password, '%systemMailAddress%': systemMailAddress} from "mails" %}
MessageFromUser
{% endtrans %}
